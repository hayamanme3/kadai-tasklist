<h1>id: <%= @タスクリスト.id %> の編集ページ</h1>

<%= form_for(@tasklist) do |f| %>
  <%= f.label :content, 'タスk' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', messages_path %>